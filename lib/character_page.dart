import 'package:flutter/material.dart';

class CharacterPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('hero icon, base stats, einzigartige, skills, food, gear (details), links to, assocaition with, outfits'),
    );
  }
}
