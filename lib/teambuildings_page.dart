import 'package:flutter/material.dart';

class TeambuildingsPage extends StatelessWidget {
  const TeambuildingsPage();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Teambuilding Page'),
    );
  }
}
