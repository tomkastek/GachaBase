import 'package:flutter/material.dart';

class GuidesPage extends StatelessWidget {
  const GuidesPage();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Guides: Youtube verlinken? -> Blog & Youtube'),
    );
  }
}
